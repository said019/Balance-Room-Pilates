# 2707 Altitud: dominio y Mercado Pago — 2026-09-24

## Infraestructura aplicada

Hostinger conserva sus nameservers apollo/athena.dns-parking.com. Se sustituyeron únicamente los registros de parking del dominio nuevo:

| Tipo | Nombre | Destino | TTL |
| --- | --- | --- | --- |
| ALIAS | @ | ctxdik1p.up.railway.app | 300 |
| CNAME | www | 88fogitw.up.railway.app | 300 |
| CNAME | api | 971ynzo3.up.railway.app | 300 |

Los tres TXT de propiedad `_railway-verify`, `_railway-verify.www` y `_railway-verify.api` están guardados con el valor exacto que devuelve `railway domain status --json`. Los tres resolvieron públicamente. El conector domain_status omite estos tokens: consultar CLI si queda VALIDATING_OWNERSHIP pese a CNAME propagado.

Web, app y admin compartirán https://2707altitud.com.mx; www es alias del mismo servicio y API usa https://api.2707altitud.com.mx/api. No se contrataron servicios de correo ni se cambiaron registros de otros dominios.

Se actualizó CORS del backend para apex/www y el hostname Railway existente. Se redeplegó el mismo build existente (deployment 8c5d233c-7a17-464f-9424-f0749404d368 SUCCESS), sin publicar código de auditoría ni ejecutar migraciones nuevas. Health en hostname Railway respondió200; preflight CORS del nuevo origen respondió204 con origen y credentials correctos.

Variables frontend VITE_API_URL y API_PROXY_TARGET apuntan a la nueva API; VITE_API_URL requiere próximo build autorizado. El frontend publicado conserva su build anterior y puede continuar llamando al hostname Railway, aceptado por CORS. Este cambio no equivale a publicar los cambios locales de Eventos/auditoría.

## Mercado Pago — pendiente de activación

Backend implementa Checkout Pro en rama de auditoría local. Requiere MP_ACCESS_TOKEN, MP_WEBHOOK_SECRET, MP_COLLECTOR_ID y MP_MODE, además de activación explícita MP_CHECKOUT_ENABLED=true. Mantener apagado hasta aceptación sandbox y despliegue autorizado con migración036. No introducir secretos en Git.

APP_PUBLIC_URL=https://2707altitud.com.mx y BACKEND_URL=https://api.2707altitud.com.mx/api. Webhook previsto: POST /api/webhooks/mercadopago. El retorno del checkout nunca activa una membresía por sí solo.

Pruebas locales usan proveedor simulado y base desechable; no demuestran entrega real del proveedor. Sandbox firmado, conciliación y recuperación de incidencias siguen pendientes. No se realizaron cargos reales ni se activaron proveedores.

## Verificación final

Los tres dominios verificados en Railway, certificado CERTIFICATE_STATUS_TYPE_VALID. HTTPS200 en apex, www, /login, /app, /admin y API /api/health; las rutas SPA200 prueban entrega del frontend, no sesiones autorizadas. Login renderizado en Chrome con el dominio nuevo. CORS API nuevo204 con Allow-Origin apex y credentials=true. Evidencia detallada: .audit-20260919/evidence/domain-20260924/https.json.

El supervisor ejecutó 16 casos locales HTTP+SQL de Mercado Pago (mp-supervisor-final.json), incluido concurrencia y ledger, y compilación TypeScript correcta. Ningún pago real. El veredicto global de auditoría sigue NO-GO hasta cerrar los pendientes ya documentados.
