// Member UI uses the same approved geometry as the administrator.
export * from '@radix-ui/react-icons';
export { Calendar as CalendarIcon, Calendar as ReaderIcon, User as PersonIcon, Bell as BellIcon, IdCard as CardStackIcon, Users as RocketIcon, Clock as ClockIcon, Settings as GearIcon } from './icons';
